package interwiew;

public class Char_Integer {
    public static void main(String[] args) {
        // How to convert Char to Integer in Java?

            // Initializing a character(ch)
            char c = '9';
            // Converting the character to an integer value
        Integer number=Integer.parseInt(String.valueOf(c));
        System.out.println(number);

        //int number1 = Integer.parseInt(String.valueOf(c));
        System.out.println(number);

    }
}

